# Visura

Visura is a Python library for declarative, replayable image generation specs.

The v0 surface is intentionally small:

```bash
visura validate examples/my-headshot.visura.toml
```

Rendering comes after the spec, loader, and compiler surface are solid.
